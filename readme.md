# Servereinrichtung
* nodeJS installieren
* npm update -g
* npm install socket.io -g
* npm install forever -g
* npm install

# Starten
* node srv/server.js

# Bei Änderungen
* forever --watch srv/server.js
* grunt watch
* /dst/*.min.* -> /dst/*.*
* grunt pack